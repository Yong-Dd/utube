extends layouts/main
include mixin/videoBlock

block content
    .videos
        each item in dbVideos
            //- h1=item.title
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })